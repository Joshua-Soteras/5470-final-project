# This file marks the tests/ directory as a Python package.
# pytest discovers test files automatically without it, but including it
# makes imports from sibling test files work cleanly if you ever add them.
